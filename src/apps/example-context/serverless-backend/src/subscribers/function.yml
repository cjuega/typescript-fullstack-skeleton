subscribers:
  handler: src/subscribers/subscribers.on
  events:
    - eventBridge:
        eventBus: !GetAtt DomainEventBus.Name
        pattern:
          detail-type:
            - company.service.1.event.exampleAggregate.created
  tags:
    ServiceName: ${env:SERVICE_NAME}
    Env: ${self:provider.stage}
